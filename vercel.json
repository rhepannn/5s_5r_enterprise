{
  "installCommand": "npm install --include=dev",
  "buildCommand": "cd apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/((?!assets/|icon-|.*\\.[a-zA-Z0-9]+$).*)", "destination": "/index.html" }
  ]
}
